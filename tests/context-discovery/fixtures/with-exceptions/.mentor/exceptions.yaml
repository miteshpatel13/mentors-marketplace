- id: exc-1
  reason: legacy endpoint pending deprecation
  rule: pagination-required
  scope: GET /v1/legacy-report
  owner: api-team
- id: exc-2
  reason: internal-only diagnostics endpoint
  rule: rate-limiting
  scope: GET /internal/diagnostics
  owner: platform-team
